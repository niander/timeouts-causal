# locally({
# to.remove.suffix <- as.character(exprs(new.games.list, new.games.teams, new.games.df,
#                                        games.perposs, games.inflscore, timeouts.inflscore,
#                                        contr.inflscore, timeout.effect.data))
# to.remove <- paste(config$seasons, 
#                    rep(to.remove.suffix, each = length(config$seasons)),
#                    sep = ".")
# 
# env_unbind(global_env(), to.remove)
# })

